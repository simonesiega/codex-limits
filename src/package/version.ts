/**
 * @fileoverview Package entry-point support for version. This module exposes the smallest runtime surface needed by the corresponding consumer.
 */
/** Package version kept in sync with package.json by package contract tests. */
export const PACKAGE_VERSION = "1.2.0";
